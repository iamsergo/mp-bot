const PredictionDataFromText = require('./prediction-data-from-text');
const DiffForScore = require('./diff-for-score');
const { default: ResultForScore } = require('./result-for-score');
const TotalForScore = require('./total-for-score');

module.exports = {
  PredictionDataFromText,
  DiffForScore,
  ResultForScore,
  TotalForScore,
};
